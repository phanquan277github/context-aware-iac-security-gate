# IaCSecBench case ENC-UNENCRYPTED-DATABASE-VULN
# Control : ENC_UNENCRYPTED_DATABASE (Managed database encryption at rest)
# Expected: VIOLATION
# Rationale: Database instance storage is unencrypted.
#
# Generated by benchmark/generate_corpus.py. Edit the specification
# there rather than this file, so the vulnerable/compliant pair stays
# minimal and the annotation stays consistent.

terraform {
  required_version = ">= 1.9.0"
  required_providers {
    aws = {
      source  = "hashicorp/aws"
      version = "~> 5.0"
    }
  }
}

resource "aws_kms_key" "target" {
  description             = "Benchmark database key"
  enable_key_rotation     = true
  deletion_window_in_days = 7
}

resource "aws_db_instance" "target" {
  identifier              = "${var.name_prefix}-enc-db"
  allocated_storage       = 20
  engine                  = "postgres"
  instance_class          = "db.t3.micro"
  username                = "benchmark"
  manage_master_user_password = true
  skip_final_snapshot     = true
  publicly_accessible     = false
  storage_encrypted       = false
}
